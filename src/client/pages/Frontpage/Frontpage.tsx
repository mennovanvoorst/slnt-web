import { Container } from "@chakra-ui/react";

const Frontpage = (): JSX.Element => (
  <Container maxW="container.2xl" centerContent>
    yo this is a frontpage
  </Container>
);

export default Frontpage;
